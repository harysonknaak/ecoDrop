import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: "row",
    padding: 8,
  },
  tittle: {
    margin: 20,
    fontSize: 22,
    fontWeight: 'bold',
    textAlign: 'center'
  }
});

export default styles