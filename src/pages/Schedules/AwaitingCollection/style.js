import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: "row",
    backgroundColor: '#0ED028',
    padding: 8,
  },
  subTittle: {
    margin: 20,
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'left'
  },
});

export default styles